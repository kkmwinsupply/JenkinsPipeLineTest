pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Build steps
            }
        }
        
        stage('Run Tests') {
            agent none
            steps {
                // Steps to run tests on the Jenkins controller
            }
        }
        
        stage('Deploy') {
            steps {
                // Deployment steps
            }
        }
    }
}
